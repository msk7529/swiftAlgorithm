//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/05/11.
//
/*
import Foundation

let input = readLine()!.split(separator: " ").map { Int(String($0))! }
let prime = getPrimeList(to: input[1])
var result: String = ""

for i in input[0]...input[1] {
    if prime[i] {
        // 이 문제처럼 출력이 매우 많은 경우, for문에서 한 줄씩 출력하는 것보다 한번에 출력하는게 시간이 훨씬 절약됨
        result += "\(i)\n"
    }
}
print(result)
*/
