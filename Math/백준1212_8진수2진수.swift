//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/05/24.
//
/*
let num = readLine()!.map { Int(String($0))! }

if num.count == 1 && num.first! == 0 {
    print("0")
} else {
    for (index, curr) in num.enumerated() {
        if index == 0 {
            print(String(curr, radix: 2), terminator: "")
        } else {
            var currNum = String(curr, radix: 2)
            
            while currNum.count != 3 {
                currNum = "0" + currNum
            }
            print(currNum, terminator: "")
        }
    }
}
*/
