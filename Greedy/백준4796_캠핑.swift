//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/08/15.
//
/*
var tc: Int = 1

while true {
    let line = readLine()!.split(separator: " ").map { Int(String($0))! }
    if line[0] == 0 { break }
    let L = line[0], P = line[1], V = line[2]
    
    let q = V / P
    print("Case \(tc): \(L * q + min(L, V - q * P))")
    
    tc += 1
}
*/
