//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/06/09.
//
/*
let T: Int = Int(readLine()!)!
for _ in 0..<T {
    let line = readLine()!.split(separator: " ").map { String($0) }
    let repeatCount: Int = Int(line[0])!
    for curr in line[1] {
        for _ in 0..<repeatCount {
            print(curr, terminator: "")
        }
    }
    print("")
}
*/
