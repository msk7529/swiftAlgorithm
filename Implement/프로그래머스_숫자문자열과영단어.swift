//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/07/19.
//
/*
var result: String = ""

func solution(_ s: String) -> Int {
    let numbers: [String] = ["zero", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine"]
    
    var tmp: String = ""
    for curr in s {
        if curr.isNumber {
            result.append(String(curr))
        } else {
            tmp.append(String(curr))
            
            if let index = numbers.firstIndex(of: tmp) {
                result.append(String(index))
                tmp = ""
            }
        }
    }
    
    return Int(result)!
}
*/
