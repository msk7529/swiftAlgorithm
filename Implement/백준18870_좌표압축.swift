//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/10/03.
//
/*
let N = Int(readLine()!)!
let arr = readLine()!.split(separator: " ").map { Int(String($0))! }
var dic: [Int: Int] = [:]
var result: String = ""

var index = 0
for curr in Array(Set(arr)).sorted() {
    dic[curr] = index
    index += 1
}

for curr in arr {
    result += String(dic[curr]!) + " "
}
result.removeLast()
print(result)
*/
