//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/10/02.
//
/*
let N = Int(readLine()!)!
for i in 1...N {
    var str = String(repeating: "*", count: 2 * (N - i) + 1)
    let emptyStr = String(repeating: " ", count: (2 * N - 1 - str.count) / 2)
    str = emptyStr + str
    print(str)
}
for i in stride(from: 2, through: N, by: 1) {
    var str = String(repeating: "*", count: 2 * (i - 1) + 1)
    let emptyStr = String(repeating: " ", count: (2 * N - 1 - str.count) / 2)
    str = emptyStr + str
    print(str)
}
*/
