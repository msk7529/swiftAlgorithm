//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/10/17.
//
/*
let N = Int(readLine()!)!
var arr: [String] = []

for _ in 0..<N {
    arr.append(readLine()!)
}

for curr in arr {
    let reverse = String(curr.reversed())
    if arr.contains(reverse) {
        let len = reverse.count
        let char = Array(curr)[len / 2]
        print("\(len) \(char)")
        break
    }
}
*/
