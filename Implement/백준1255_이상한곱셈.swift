//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/12/22.
//
/*
let line = readLine()!.split(separator: " ").map { String($0) }
let num1 = line[0], num2 = line[1]
let firstNum = num1.compactMap { $0.wholeNumberValue }, secondNum = num2.compactMap { $0.wholeNumberValue }
var result: Int = 0

for curr1 in firstNum {
    for curr2 in secondNum {
        result += curr1 * curr2
    }
}
print(result)
*/
