//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/12/13.
//
/*
let N = Int(readLine()!)!
var dic: [String: Int] = [:]
var result: [String] = []

for _ in 0..<N {
    let word = readLine()!
    dic[word, default: 0] += 1
}

let maxValue = dic.values.max() ?? 0

for (key, value) in dic {
    if value == maxValue {
        result.append(key)
    }
}
result.sort()
print(result.first!)
*/
