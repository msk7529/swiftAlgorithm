//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/07/30.
//
/*
var result: Int = 0

for i in 0..<8 {
    let line = readLine()!
    let firstIsWhite = i.isMultiple(of: 2)
    
    for (index, value) in line.enumerated() {
        if firstIsWhite && index.isMultiple(of: 2) && value == "F" {
            result += 1
        } else if !firstIsWhite && !index.isMultiple(of: 2) && value == "F" {
            result += 1
        }
    }
}

print(result)
*/
