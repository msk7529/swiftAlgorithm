//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/08/22.
//
/*
var num = Array(readLine()!.map { String($0) })
var index = 0
var result: String = ""

while num.count % 3 != 0 {
    num.insert("0", at: 0)
}

while index < num.count {
    let str = num[index..<index+3].joined()
    let strToDecimal = Int(str, radix: 2)!
    result.append(String(strToDecimal, radix: 8, uppercase: false))
    index += 3
}

print(result)
*/
