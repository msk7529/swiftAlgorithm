//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2022/02/21.
/*
func getUniqueCharacter(s: String) -> Int {
    var dic: [Character: Int] = [:]
    for char in s {
        if let value = dic[char] {
            dic[char] = value + 1
        } else {
            dic[char] = 1
        }
    }
    
    for (index, char) in s.enumerated() {
        if let value = dic[char], value == 1 {
            return index + 1
        }
    }
    
    return -1
}
*/
