//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2022/01/16.
/*
let N = Int(readLine()!)!
var arr: [Int] = []
var trash: [Int] = []
var result: String = ""

for num in stride(from: N, through: 1, by: -1) {
    arr.append(num)
}

while arr.count > 1 {
    let last = arr.removeLast()
    trash.append(last)
    let last2 = arr.removeLast()
    arr.insert(last2, at: 0)
}

for num in trash {
    result += "\(num) "
}
result += "\(arr[0]) "
result.removeLast()

print(result)
*/
