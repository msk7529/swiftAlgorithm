//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/08/16.
//
/*
let doc = readLine()!.map { $0 }
let word = readLine()!.map { $0 }
var index: Int = 0
var result = 0

while index + word.count <= doc.count {
    if Array(doc[index..<index+word.count]) == word {
        result += 1
        index += word.count
    } else {
        index += 1
    }
}

print(result)
*/
