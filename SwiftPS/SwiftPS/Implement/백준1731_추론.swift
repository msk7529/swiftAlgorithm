//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2022/01/21.
/*
let N = Int(readLine()!)!
var arr: [Int] = []
var Q: Int = 0
var result: Int = 0

for _ in 0..<N {
    arr.append(Int(readLine()!)!)
}

if arr[1].isMultiple(of: arr[0]) {
    let num = arr[1] / arr[0]
    if arr[1] * num == arr[2] {
        Q = num
        result = arr.last! * Q
    } else {
        Q = arr[1] - arr[0]
        result = arr.last! + Q
    }
} else {
    Q = arr[1] - arr[0]
    result = arr.last! + Q
}
print(result)
*/
