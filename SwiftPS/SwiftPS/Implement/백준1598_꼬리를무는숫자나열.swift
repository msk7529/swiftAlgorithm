//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2022/01/15.
/*
import Foundation

let line = readLine()!.split(separator: " ").map { Int(String($0))! }
let (y1, x1) = calcPos(num: line[0])
let (y2, x2) = calcPos(num: line[1])

print(abs(y1 - y2) + abs(x2 - x1))

func calcPos(num: Int) -> (Int, Int) {
    let x = num.isMultiple(of: 4) ? num / 4 - 1 : num / 4
    var y = (num % 4) - 1
    if y < 0 { y = 3 }
    return (y, x)
}
*/
