//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/07/17.
//
/*
var arr: [UInt8: Int] = [:]
let word = readLine()!

for curr in word {
    if let value = arr[curr.asciiValue!] {
        arr[curr.asciiValue!] = value + 1
    } else {
        arr[curr.asciiValue!] = 1
    }
}

for i in 97...122 {
    if let value = arr[UInt8(i)] {
        print(value, separator: " ")
    } else {
        print(0, separator: " ")
    }
}
*/
