//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2022/08/23

/*
 수직선 생각하면 금방 풀 수 있음. 1번 난이도?
 */

/*
func findMaxNum(x: Int, y: Int, z: Int) -> Int {
    let diff = abs(x - y)
    if z < diff {
        return -1
    }
    return max(x, y) + (z - diff) / 2
}

print(findMaxNum(x: 8, y: 5, z: 3))
print(findMaxNum(x: 4, y: 4, z: 6))
*/
