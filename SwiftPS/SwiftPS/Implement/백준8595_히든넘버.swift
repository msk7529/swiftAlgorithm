//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/07/30.
//
/*
let N: Int = Int(readLine()!)!
let word = readLine()! + "a"
var arr: [String] = []
var result: Int = 0
var tmp: Int = 0

for curr in word {
    if curr.isNumber {
        let toNum = Int(curr.asciiValue! - 48)
        if tmp == 0 {
            tmp = toNum
        } else {
            tmp = tmp * 10 + toNum
        }
    } else {
        if tmp != 0 {
            result += tmp
            tmp = 0
        }
    }
}

print(result)
*/
