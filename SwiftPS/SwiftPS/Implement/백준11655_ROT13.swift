//
//  main.swift
//  Algorithm
//
//  Created by MinSeop on 2021/08/19.
//
/*
let str = readLine()!
var result: String = ""

str.forEach {
    if $0.isUppercase {
        var ascii = $0.asciiValue! + 13
        ascii = ascii > 90 ? ascii - 26 : ascii
        result += String(UnicodeScalar(ascii))
    } else if $0.isLowercase {
        var ascii = $0.asciiValue! + 13
        ascii = ascii > 122 ? ascii - 26 : ascii
        result += String(UnicodeScalar(ascii))
    } else {
        result += String($0)
    }
}

print(result)
*/
